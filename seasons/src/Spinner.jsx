import "./Spinner.css";
import React from "react";

const Spinner = () => {
   return (
      <div className="ui container main">
         <i className="spinner loading icon massive" />
      </div>
   );
};

export default Spinner;
